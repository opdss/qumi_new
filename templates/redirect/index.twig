{% extends 'console.twig' %}

{% block console %}
    <div class="row">
        <div class="col-md-12">
        <div>
            <div class="opts">
                    <form class="form-inline" action="{{ path_for('redirect') }}" method="get" id="filter_form">
                        <div class="form-group">
                            <input type="text" class="form-control" name="kw" placeholder="搜索关键词" value="{{ filter.kw }}">
                        </div>
                        <div class="form-group">
                            <select  class="selectpicker" name="redirect_status">
                                <option value="0">跳转状态</option>
                                <option value="301" {{ filter.redirect_status==301 ? 'selected' : '' }}>301</option>
                                <option value="302" {{ filter.redirect_status==302 ? 'selected' : '' }}>302</option>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary">查询</button>
                        <a href="javascript:add()" class="btn btn-info">添加跳转记录</a>
                    </form>
            </div>
            <div class="table">
                <table class="table table-striped table-hover table-flex">
                    <thead>
                    <tr>
                        <td class="col-md-1"><label><input type="checkbox" class="check-all"> 全选</label></td>
                        <td>标题</td>
                        <td class="col-md-2">
                            <a href="javascript:void(0)"
                               class="order_by"
                               data-order_name="domain_name"
                               data-val="{{ filter.order_by.domain_name ? (filter.order_by.domain_name == 'asc' ? 'desc' : 'asc') : 'desc'}}">
                                源地址
                                <span class="glyphicon glyphicon-sort"></span>
                            </a>
                        </td>
                        <td class="col-md-2">
                            <a href="javascript:void(0)"
                               class="order_by"
                               data-order_name="redirect_url"
                               data-val="{{ filter.order_by.redirect_url ? (filter.order_by.redirect_url == 'asc' ? 'desc' : 'asc') : 'desc'}}">
                                目标地址
                                <span class="glyphicon glyphicon-sort"></span>
                            </a>
                        </td>
                        <td>
                            <a href="javascript:void(0)"
                               class="order_by"
                               data-order_name="redirect_status"
                               data-val="{{ filter.order_by.redirect_status ? (filter.order_by.redirect_status == 'asc' ? 'desc' : 'asc') : 'desc'}}">
                                跳转类型
                                <span class="glyphicon glyphicon-sort"></span>
                            </a>
                        </td>
                        {#<td>
                            <a href="javascript:void(0)"
                               class="order_by"
                               data-order_name="redirect_status"
                               data-val="{{ filter.order_by.clicks ? (filter.order_by.clicks == 'asc' ? 'desc' : 'asc') : 'desc'}}">
                                点击数
                                <span class="glyphicon glyphicon-sort"></span>
                            </a>
                        </td>#}
                        <td>操作</td>
                    </tr>
                    </thead>
                    <tbody>
                    {% for item in records %}
                    <tr>
                        <td><input type="checkbox" class="check-one" name="id" value="{{ item.id }}"></td>
                        <td>{{ item.title }}</td>
                        <td>{{ item.prefix~'.'~item.domain_name }}</td>
                        <td>{{ item.redirect_url }}</td>
                        <td>{{ item.redirect_status }}</td>
                        {#<td>{{ item.clicks }}</td>#}
                        <td>
                            <a href="javascript:edit([{{ item.id }}])">编辑</a>
                            <a href="javascript:del({{ item.id }})">删除</a>
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
                <div class="row">
                    {#<label><input type="checkbox" class="check-all"> 全选</label>#}
                    <div class="col-md-5" style="padding: 20px 0;padding-left: 20px">
                        <label>
                            <input type="checkbox" class="check-all">&nbsp;全部
                        </label>
                        <button class="btn btn-default more-del">批量删除</button>
                    </div>
                    <div class="col-md-7 text-right">
                        {{ pagination|raw }}
                    </div>
                </div>
            </div>
        </div>
        </div>
    </div>

{% endblock %}

{% block script %}
    <script type="text/javascript">
        function add() {
            var modalObj = loadModal({remote:'{{ path_for('redirect.modal', {'act':'add'}) }}', backdrop: 'static', keyboard: false}, 0);
            modalObj.on('hidden.bs.modal', function (e) {
                this.remove();
            });
        }

        function edit(ids) {
            if (!ids) {return false;}
            var modalObj = loadModal({remote:'{{ path_for('redirect.modal', {'act':'edit'}) }}?redirect_id='+ids, backdrop: 'static', keyboard: false});
            modalObj.on('hidden.bs.modal', function (e) {
                this.remove();
            });
        }

        function del(ids) {
            if (!ids) {return false;}
            swal({
                    title: "确定删除选中项吗",
                    text: "删除将不在提供转发服务",
                    type: "warning",
                    showCancelButton: true,
                    closeOnConfirm: false,
                    cancelButtonText: '取消',
                    confirmButtonText : '确认删除',
                    showLoaderOnConfirm: true,
                },
                function(){
                    $.get('{{ path_for('redirect.del') }}', {'redirect_id':ids}, function (res) {
                        if (res.errCode == 0) {
                            utils.swal.success('', true);
                        } else {
                            utils.swal.error(res.errMsg);
                        }
                    })
                });
        }
        $('.more-del').click(function () {
            del(getChecked($(this)));
        });

        $('.more-edit').click(function () {
            edit(getChecked($(this)));
        })

        $('.more-dns').click(function () {
            dnsCheck(getChecked($(this)));
        })

    </script>
{% endblock %}